# Base
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/CyberShield
JWT_SECRET=DDND82893JMV20N02VM20@@#
SESSION_SECRET=N3I1733VM@#~2VEQJ13
PORT=3001
NODE_ENV=development

# SSH (Kali Linux)
SSH_HOST=10.10.10.X
SSH_PORT=22
SSH_USER=kali
SSH_PASS=kali

# Wazuh
WAZUH_HOST=10.10.10.49
WAZUH_USER=admin
WAZUH_PASS=admin

# n8n
N8N_WEBHOOK_URL=http://localhost:5678

# Google OAuth (opcional — sin esto el botón de Google no aparece)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# SMTP (Para emails de verificación y reseteo de contraseña)
# INFO: Si dejas SMTP_USER y SMTP_PASS vacíos, el backend usará el modo simulado
# y mostrará los enlaces de verificación y recuperación directamente en la consola.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
